// Shim: project permission config loading lives in the permissions spine.
export {
  loadInnocenceConfig,
  parseRuleSpec,
  rulesFromConfig,
  type InnocenceConfig,
  type McpServerConfig,
  type ProjectPermissionConfig,
} from "@innocencecode/harness-permissions";
