// Shim: the permission policy vocabulary lives in the permissions spine.
export type {
  AskResponse,
  PermissionDecision,
  PermissionMode,
  PermissionRequest,
  PermissionResource,
  PolicyRule,
  RuleVote,
  ToolCallInfo,
} from "@innocencecode/harness-permissions";
